Mzani (Swahili for Balance) is a library for creating a load balencer server using nothing but std rust.

I didn't implement an async runtime either, however a thread pool model is used to handle concurrent requests
in an efficient manner.
